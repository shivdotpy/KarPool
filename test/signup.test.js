import react from 'react';

test('should render signup component', () => {
    // const component = shallow(<Signup />);
    // const signupComponent = component.find(`[data-test="component-signup"]`)
    // expect(signupComponent.length).toBe(1)
});
